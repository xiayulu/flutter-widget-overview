import 'package:flutter/material.dart';

class ExCenter extends StatelessWidget {
  const ExCenter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Center me'));
  }
}
